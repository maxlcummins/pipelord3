
file_of_file_names: config/fofn.txt

#Input type ['raw_reads', 'filtered_reads', 'assemblies']
input_type: 'raw_reads'

#File paths
input_dir: reads
output_dir: output

#Recommended flags
#Read trimming - default = True
trim_reads: True

#Shovill assembler
assembler: spades

#Assembly-stats: To disable, set to False
assembly_stats: True #False

